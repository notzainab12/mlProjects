# in setup.py, whenever find_packages() is running, it will just go and see 
# "in how many folders you have __init__.py file"
# So it will consider the folder (src) as a package and will build the package accordingly.